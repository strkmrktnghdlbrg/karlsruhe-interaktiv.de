---
import type { Hotel } from "../data/hotels";
interface Props { hotel: Hotel; locale?: "de" | "en" }
const { hotel, locale = "de" } = Astro.props;
const en = locale === "en";
---

<div class="p-5 sm:p-6 border" style="background: var(--color-surface); border-color: var(--color-border); border-radius: var(--radius-card);">
  <p class="eyebrow mb-2">{en ? "Book now" : "Direkt buchen"}</p>
  <div class="flex items-baseline gap-2 mb-4">
    <span class="text-3xl font-bold" style="color: var(--color-text);">{en ? `from ${hotel.priceFrom} EUR` : `${hotel.priceFrom} EUR`}</span>
    <span class="text-sm" style="color: var(--color-text-muted);">{en ? "/ night" : "/ Nacht ab"}</span>
  </div>
  <a href={hotel.bookingUrl} rel="nofollow noopener sponsored" target="_blank" class="btn-primary w-full">
    {en ? "Check on Booking.com" : "Auf Booking.com pruefen"} &rarr;
  </a>
  <p class="text-[11px] mt-3 leading-relaxed" style="color: var(--color-text-muted);">
    {en
      ? "* Affiliate link: if you book through this link we earn a commission, at no extra cost to you."
      : "* Affiliate-Link: Beim Buchen ueber diesen Link erhalten wir eine Provision, du zahlst keinen Cent mehr."}
  </p>
</div>
